import type { ImageLayerData } from '../../types';

// Placeholder until the three-pane editor lands.
export function MorphEditor(_: { layer: ImageLayerData; sceneIndex: number; onClose: () => void }) {
  return null;
}
